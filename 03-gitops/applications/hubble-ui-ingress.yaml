apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hubble-ui-ingress
  namespace: argocd
spec:
  destination:
    namespace: kube-system
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/SerhiiMyronets/homelab-talos-kubernetes-terraform
    path: 03-gitops/apps/hubble-ui-ingress
    targetRevision: HEAD
    directory:
      recurse: true
  syncPolicy:
    automated:
      prune: true
      selfHeal: true